{% extends 'base.html.twig' %}

{% block title %}Hello MainController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>


    {% if app.user %}
        <h3>Date du jour : {{ 'now' | date('d/m/Y', 'Europe/Paris') }}</h3>
        <h3>Participant : {{ app.user.username }}</h3>
        <div class="affichage sortie">

        </div>
        <a class="link_button btn btn-outline-secondary" href="{{ path('sortie_create') }}" role="button">Créer une sortie</a>

    {% else %}
        <div class="example-wrapper">
            <h1>Hello Icc c'est la page d'accueil avec plein de filtres !! </h1>
            <a class="link_button btn btn-outline-secondary" href="{{ path('app_login') }}" role="button">Connecte toi</a>
        </div>


    {% endif %}

{% endblock %}
